spring:
  application:
    name: Void
  jpa:
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
  jackson:
    mapper:
      ACCEPT_CASE_INSENSITIVE_PROPERTIES: true
  threads:
    virtual:
      enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health